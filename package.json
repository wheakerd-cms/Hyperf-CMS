{
    "name": "document",
    "scripts": {
        "install": "npm install docsify-cli --save-dev",
        "docs": "docsify serve ./docs"
    },
    "engines": {
        "node": ">=22.0",
        "npm": ">=10.0"
    }
}
